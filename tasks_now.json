[
  "ENVIRONMENT - read before running any command. Your workspace is the agentic_HITL_harness repo. A bare `python` resolves to the SYSTEM interpreter, which has NO langgraph, so `python -m pytest` fails with an ImportError unrelated to the code. ALWAYS use `.venv/Scripts/python.exe`. Test command: .venv/Scripts/python.exe -m pytest apps/harness/tests -q  -- currently 103 passed. If you see an ImportError for langgraph you used the wrong interpreter; do not try to fix the repo. exec_command has a 300s timeout. NODE IS INSTALLED (node + npm + npx).",

  "MODEL SPEED - this changes how you should work. You are running devstral:24b at about 3.4 tokens per second. A turn that emits a 2000-token file takes about TEN MINUTES. Never write a large file in one turn: write a small piece, verify it, then extend. Read only the specific files you need - do not browse the repo.",

  "LAST ROUND - what happened. Your Task 3 (prune dry-run) was correctly SKIPPED because test_cli.py already had test_prune_defaults_to_dry_run. That was the right call and exactly what was asked. Your Task 2 was NOT completed - no test anywhere reads packages/contracts/ts/PassiveCard.schema.d.ts. It is re-issued below as Task 1 with tighter steps. Do not repeat Task 3.",

  "STILL TRUE - do not redo, do not revert. GET /api/cards/{card_id}/why exists. cmd_serve is SYNCHRONOUS and must stay so (ADR 35). THE CONTRACT CODEGEN RUNS FOR REAL (ADR 40): packages/contracts/ts holds 16 GENERATED .d.ts files and apps/feed/src/types/contracts.ts is a CURATED RE-EXPORT of them. Never hand-write a type that exists in packages/contracts/ts and never edit files in that directory - they are generated. Regenerate with: .venv/Scripts/python.exe -m harness.contracts.export  then  cd apps/feed && npm run contracts",

  "TASK 1 (re-issued). Guard a codegen gap: json2ts DROPS field-level descriptions for primitive fields. It emits `export type Salience = number;` with no comment, so a rule written as a `#:` comment above a field never reaches TypeScript. Such a rule only survives if it is in the model's CLASS DOCSTRING. PassiveCard's class docstring carries the ADR 43 rule that salience must never be derived from operator behaviour, and that text must keep reaching the frontend. STEP 1: run this and confirm it prints a line - .venv/Scripts/python.exe -c \"print([l for l in open('packages/contracts/ts/PassiveCard.schema.d.ts',encoding='utf-8') if 'engagement weight' in l])\"  STEP 2: open apps/harness/tests/test_contracts.py and add ONE function named test_passive_card_prohibition_reaches_typescript that reads that same file and asserts the phrase 'engagement weight' is in its text. Follow the file's existing style for locating repo paths - look at how test_generated_typescript_is_not_stale already does it and reuse that approach rather than inventing a new one. The docstring must say WHY: a client that re-sorts the feed by anything it observes about the reader reintroduces the product this one exists to replace, so the rule has to be readable where a frontend developer works. Verify: .venv/Scripts/python.exe -m pytest apps/harness/tests -q (expect 104 passed).",

  "TASK 2 (only after Task 1 passes). apps/harness/src/harness/hitl/ledger.py has get_attention_ledger_card(conn), which returns a PassiveCard built from the v_attention_ledger view, or None when there is no telemetry yet. Only the populated path is tested. Add ONE test for the EMPTY path: with a fresh database and no interaction_metric rows, get_attention_ledger_card must return None. This matters because the feed appends the ledger card only when the action queue is empty, so on a brand new install this function runs on every single feed request - and if it returned a card built from zero rows, a new operator's first ever screen would be a ledger reading zero. Put the test in apps/harness/tests/test_hitl_server.py next to test_get_feed_surfaces_attention_ledger_card_when_telemetry_exists and reuse the same fixture. Verify with the same pytest command (expect 105 passed).",

  "GUARDRAILS for every task: read AGENTS.md first; data travels in JSON bodies, never query strings; source is ASCII-only, no em-dashes or smart quotes; never decorate a HarnessModel subclass with @dataclass (ADR 34); never edit generated files under packages/contracts/ts. DO NOT touch apps/feed - Gemini is actively rewriting the client's type boundary there. If a task turns out to be already done, SAY SO AND STOP; reporting work you did not do is the worst possible outcome."
]
